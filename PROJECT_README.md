# 充电站净利润风险仿真系统 🔌⚡

> **数据来源**：深圳市公共充电站真实数据集 UrbanEV（CC0 协议）  
> **技术栈**：Python FastAPI · M/M/c 基线 + M/G/c 修正 · 蒙特卡洛仿真 · ECharts 5 可视化  
> 原始数据文档见 [readme.md](./readme.md)

---

## 全局架构与数据工作流

```
[👨‍💼 用户/评委]
      │
      ▼ (1. 拖动敏感性分析滑块，如：调整服务费 +20%)
┌──────────────────────────────────────────────────────┐
│ 🖥️ 前端展示层  (Frontend UI — 本地 5500 端口)          │
│   ├── index.html   呈现毛玻璃卡片与图表容器             │
│   ├── style.css    平滑过渡动画 / Apple 暗色风格        │
│   └── app.js       监听滑动事件，收集参数，发起 Fetch   │
└──────┬───────────────────────────────────────────────┘
       │
       ▼ (2. POST /api/run-simulation  携带 JSON)
┌──────┴───────────────────────────────────────────────┐
│ ⚙️ 后端调度层  (FastAPI — 本地 8000 端口)              │
│   ├── main.py      接收请求、调度计算、打包 JSON        │
│   └── schemas.py   Pydantic 校验参数合法性             │
└──────┬───────────────────────────────────────────────┘
       │
       ▼ (3. 验证通过，派发计算任务)
┌──────┴───────────────────────────────────────────────┐
│ 🧠 核心算法层  (Python 科学计算)                       │
│   ├── data_processor.py   读取并缓存基准数据 📊        │
│   │         ▲ 读取                                   │
│   │   📁 data/ (5 个核心 CSV)                         │
│   ├── queuing_model.py    M/M/c 基线 + M/G/c 修正      │
│   └── monte_carlo.py      1000 次净利润随机迭代         │
└──────┬───────────────────────────────────────────────┘
       │
       ▼ (4. 将 1000 次数组 / VaR / 利用率打包为 JSON)
[📈 前端大屏直方图平滑过渡，VaR 数值变色跳动，完成交互闭环]
```

---

## 项目文件树

```
小组作业/
├── data/                        # 数据层（只读）
│   ├── inf.csv                  # 站点元数据（桩数、坐标、TAZ）
│   ├── duration.csv             # 小时级充电时长（累计会话时长，单位：h）
│   ├── occupancy.csv            # 小时级平均并发充电数
│   ├── volume-11kW.csv          # 小时级充电电量（kWh，11kW 估算）
│   ├── e_price.csv              # 客户侧小时级电价（元/kWh）
│   └── s_price.csv              # 小时级服务费（元/kWh）
├── backend/                     # 后端引擎层
│   ├── requirements.txt         # Python 依赖
│   ├── data_processor.py        # 数据提炼 → 基准参数缓存
│   ├── queuing_model.py         # M/M/c 基线 + M/G/c 修正计算器
│   ├── monte_carlo.py           # 1000 次财务风险仿真
│   ├── schemas.py               # Pydantic 接口安检员
│   └── main.py                  # FastAPI 主机 & 路由
├── frontend/                    # 前端展示层
│   ├── index.html               # 页面骨架（Grid + 滑块 + 图表容器）
│   ├── style.css                # Apple 风格暗色毛玻璃皮肤
│   └── app.js                   # 前端逻辑 & ECharts 交互
├── PROJECT_README.md            # 本文件 — 新项目全流程说明
├── readme.md                    # 原始数据集说明（UrbanEV）
├── tutorial.python.md           # Python 全流程代码讲解
└── start.sh                     # 一键启动脚本
```

---

## 为什么采用 M/M/c 基线 + M/G/c 修正

### 模型适用性论证

本项目不再停留在纯 M/M/c，而是采用两步法：先用 M/M/c 作为可解释、可解析的**基线模型**，再用经验变异系数 CV 对等待时间做 **M/G/c 近似修正**。下面先说明为什么保留 M/M/c 基线，再解释为什么必须加上 M/G/c 修正。

#### 假设一：到达过程服从泊松分布（$M$/·/·）

> **数据支撑**：`occupancy.csv` 提供每小时每 TAZ 的并发充电会话数 $L$，结合 `duration.csv` 推导的平均会话时长 $W$，通过 **Little 定律** $\lambda = L/W$ 估算每站到达率约 **2.83 次/小时**。
>
> **理论依据**：充电需求由大量相互独立的用户产生，每个用户的充电决策互不影响，符合泊松过程的"独立增量"与"稀疏性"条件。工程领域中，大量用户独立触发的服务请求（电话呼叫、Web 请求、EV 充电）均被证实近似泊松到达。
>
> **颗粒度限制**：本数据集提供的是**小时级聚合统计量**，而非单条到达时间戳。这意味着我们只能验证"每小时到达次数"是否服从泊松分布，无法直接验证到达间隔是否服从指数分布。在此粒度下，泊松假设作为工程近似是合理的。

#### 假设二：服务时间服从指数分布（M/$M$/·）

> **数据计算结果**：
>
> | 统计量 | 值 |
> |--------|-----|
> | 样本量 | 1,069,289 个 TAZ-小时对 |
> | 均值 $\overline{W}$ | **0.724 小时**（43.4 分钟）|
> | 标准差 | 0.264 小时 |
> | **变异系数 CV = std/mean** | **0.365** |
> | 估算 Erlang-$k$（$k \approx 1/\text{CV}^2$） | **$k \approx 7.5$** |
>
> **诚实的结论**：CV=0.365 **远小于指数分布的 CV=1**，说明充电时长分布比指数分布更集中，接近 **Erlang-8** 分布。这符合直觉——充电行为受电池容量和充电功率约束，不可能像指数分布那样出现极长尾。
>
> **为什么升级为 M/G/c 修正**：由于服务时间经验 CV=0.365，纯 M/M/c 会系统性高估等待时间。项目现已引入 Lee-Longton/Allen-Cunneen 风格近似：
>
> $$W_q(M/G/c) \approx W_q(M/M/c) \times \frac{1 + CV^2}{2}$$
>
> 代入本项目数据，修正系数为：
>
> $$\frac{1 + 0.365^2}{2} \approx 0.5666$$
>
> 即真实等待时间约为 M/M/c 基线的 **56.66%**。这既保留了 Erlang C 的解析结构，也修正了指数服务假设不成立带来的系统偏差。
>
> **颗粒度限制**：本数据为小时级 TAZ 聚合均值，而非单次会话的精确开始/结束时间戳，无法直接拟合单次服务时长的概率密度函数。

#### 假设三：$c$ 个同质服务台（·/·/$c$）

> **数据支撑**：`inf.csv` 中 `charge_count` 字段直接记录每座充电站的充电桩数量，均值 **12.87 桩/站**（中位数 10 桩，最大 106 桩），直接对应 M/M/c 中的 $c$ 参数。同质性假设（各桩服务率相同）在同一站内同型号桩之间是合理近似。

#### 综合评估

| 排队模型三要素 | 数据是否覆盖 | 颗粒度 | 偏差方向 |
|---|---|---|---|
| $\lambda$（泊松到达率）| ✅ 由 Little 定律推导 | 小时级聚合 | 轻微低估峰值 |
| $\mu$（指数服务率）| ⚠️ 可估均值，CV 不符合指数 | 小时级聚合均值 | 高估等待时间（保守）|
| $c$（服务台数）| ✅ 直接来自 `charge_count` | 站级精确值 | 无偏 |

> **结论**：最终落地模型是“**M/M/c 基线 + M/G/c 修正**”。M/M/c 负责提供可解释的解析骨架，M/G/c 修正负责把真实服务时间波动（CV=0.365）纳入等待时间计算，从而把老师最可能质疑的统计假设缺口补上。

---

## 数据层详解（data/）

> 以下展示每个 CSV 文件的前 3 行样本数据（宽表只展示前 6 列）。

### `inf.csv` — 站点元数据

**数据预览（前 3 行）：**

| station_id | longitude | latitude | charge_count | TAZID | area |
|---|---|---|---|---|---|
| 1001 | 113.784724 | 22.714121 | 20 | 559 | 5726523.056 |
| 1002 | 113.785002 | 22.725900 | 22 | 558 | 3927542.045 |
| 1003 | 113.787971 | 22.735538 | 6  | 558 | 3927542.045 |

**形状**：1362 行 × 7 列（每行一座充电站）

| 字段 | 说明 |
|------|------|
| `station_id` | 充电站 ID |
| `longitude` / `latitude` | 地理坐标（深圳 WGS84）|
| `charge_count` | **充电桩数 $c$** → 排队模型服务台参数；均值 12.87，中位数 10 |
| `TAZID` | 所属交通分析区（275 个 TAZ）|
| `area` | TAZ 面积（m²）|

**计算用途**：`mean(charge_count)≈13` → 基准 $c$；`1362/275≈4.95` → 每 TAZ 平均站数，用于将 TAZ 级数据折算到单站粒度。

---

### `duration.csv` — 充电累计时长

**数据预览（前 3 行，前 6 列）：**

| time | TAZ 102 | TAZ 104 | TAZ 105 | TAZ 106 | TAZ 107 |
|---|---|---|---|---|---|
| 2022-09-01 00:00:00 | 8.833 | 1.583 | 9.167 | 11.250 | 13.167 |
| 2022-09-01 01:00:00 | 8.167 | 1.333 | 11.000 | 11.667 | 14.417 |
| 2022-09-01 02:00:00 | 8.917 | 1.917 | 11.000 | 12.750 | 13.667 |

**形状**：4344 行（小时）× 276 列（`time` + 275 个 TAZ）

- **含义**：每 TAZ 每小时内所有并发会话的**累计**充电时长（单位：小时）
- **单次时长推导**：`session_duration = duration / occupancy`  
  → 样本均值 **0.724 h（43 min）**，CV=0.365，接近 Erlang-8 分布
- **排队参数**：$\mu = 1/\overline{W} \approx$ 1.38 次/小时/桩，`service_time_cv \approx 0.365`
- **颗粒度说明**：小时级 TAZ 聚合，**无单次会话开始/结束时间戳**，不能直接拟合服务时长分布，只能估算均值参数

---

### `occupancy.csv` — 并发充电会话数

**数据预览（前 3 行，前 6 列）：**

| time | TAZ 102 | TAZ 104 | TAZ 105 | TAZ 106 | TAZ 107 |
|---|---|---|---|---|---|
| 2022-09-01 00:00:00 | 17.0 | 1.0 | 15.0 | 54.0 | 23.0 |
| 2022-09-01 01:00:00 | 16.0 | 1.0 | 19.0 | 50.0 | 25.0 |
| 2022-09-01 02:00:00 | 16.0 | 1.0 | 20.0 | 44.0 | 24.0 |

**形状**：4344 行 × 276 列

- **含义**：每 TAZ 每小时的平均**并发**充电会话数 $L$（即系统内顾客数，包含等待+服务中）
- **排队参数**：由 Little 定律 $\lambda_{TAZ} = L / W$，再除以每 TAZ 站数 4.95 → 单站 $\lambda \approx$ **2.83 次/小时**
- **颗粒度说明**：小时级聚合平均值，**无单次到达时间戳**，不能直接验证到达间隔的指数分布，只能通过"每小时到达次数"间接验证泊松假设

---

### `volume-11kW.csv` — 充电电量

**数据预览（前 3 行，前 6 列）：**

| time | TAZ 102 | TAZ 104 | TAZ 105 | TAZ 106 | TAZ 107 |
|---|---|---|---|---|---|
| 2022-09-01 00:00:00 | 56.29 | 11.08 | 60.38 | 78.75 | 90.13 |
| 2022-09-01 01:00:00 | 50.46 | 9.33  | 72.63 | 81.67 | 100.33 |
| 2022-09-01 02:00:00 | 55.71 | 12.83 | 73.21 | 89.25 | 94.50 |

**形状**：4344 行 × 276 列

- **含义**：每 TAZ 每小时总充电量（kWh），按 11kW 额定桩功率估算
- **单次电量推导**：`kWh/次 = (volume / occupancy) × session_duration`  
  经 [5%, 95%] 截断清洗后，样本均值约 **12 kWh/次**
- **蒙特卡洛用途**：`mean_kwh` 和 `std_kwh` 输入正态分布随机采样，模拟每次充电的收益和成本

---

### `e_price.csv` — 客户侧电价

**数据预览（前 3 行，前 6 列）：**

| time | TAZ 102 | TAZ 104 | TAZ 105 | TAZ 106 | TAZ 107 |
|---|---|---|---|---|---|
| 2022-09-01 00:00:00 | 0.924 | 0.923 | 1.124 | 1.170 | 0.926 |
| 2022-09-01 01:00:00 | 0.924 | 0.923 | 1.124 | 1.170 | 0.926 |
| 2022-09-01 02:00:00 | 0.924 | 0.923 | 1.124 | 1.170 | 0.926 |

**形状**：4344 行 × 276 列

- **含义**：客户实际支付的**电费单价**（元/kWh），含峰谷分时电价，均值约 **0.90 元/kWh**
- **特点**：同一 TAZ 内按天或按段变化，不同 TAZ 之间差异显著（0.47~1.45 元/kWh）
- **蒙特卡洛用途**：提供 `mean_e_price`、`std_e_price` 用于电价随机扰动

---

### `s_price.csv` — 服务费

**数据预览（前 3 行，前 6 列）：**

| time | TAZ 102 | TAZ 104 | TAZ 105 | TAZ 106 | TAZ 107 |
|---|---|---|---|---|---|
| 2022-09-01 00:00:00 | 0.856 | 0.760 | 0.750 | 0.633 | 0.737 |
| 2022-09-01 01:00:00 | 0.856 | 0.760 | 0.750 | 0.633 | 0.737 |
| 2022-09-01 02:00:00 | 0.856 | 0.760 | 0.750 | 0.633 | 0.737 |

**形状**：4344 行 × 276 列

- **含义**：充电站向用户收取的**服务费单价**（元/kWh），均值约 **0.76 元/kWh**
- **特点**：时间变化幅度小（多数 TAZ 服务费固定），站间差异由运营商定价策略决定（0.47~1.45 元/kWh）
- **蒙特卡洛用途**：提供 `mean_s_price`、`std_s_price`；用户可通过滑块调整 `service_fee_change` 比例进行敏感性分析

---

## 后端引擎层详解（backend/）

### `data_processor.py` — 数据提炼厂

**核心函数**：`load_baseline_data() -> dict`

| 参数键 | 来源 | 说明 |
|--------|------|------|
| `c` | `inf.csv` | 平均充电桩数（int）|
| `mu` | `duration / occupancy` | 单桩服务率（次/h）|
| `service_time_cv` | `duration / occupancy` | 单次充电时长 CV，用于 M/G/c 修正 |
| `lambda_rate` | Little 定律 | 单站到达率（次/h）|
| `mean_kwh`, `std_kwh` | `volume-11kW.csv` | 单次电量均值 & 标准差 |
| `mean_e_price`, `mean_s_price` | 价格 CSV | 电价 / 服务费均值 |
| `wait_cost_per_minute` ★ | **补全参数** | **0.20 元/车·分钟**（等待导致流失/口碑折损的折现） |
| `wholesale_price` ★ | **补全参数** | **0.55 元/kWh**（深圳 2023 工商业购电均值）|
| `daily_fixed_cost` ★ | **补全参数** | **300 元/站/天**（行业中位数：人工+折旧+场地）|

> ★ **补全参数说明**：原始数据未提供上述两项，依据深圳市发改委 2023 年电价文件及头部充电运营商（星星充电、特来电）公开财报中位数补全，代码中有注释标注。

### `queuing_model.py` — 排队论计算器

**核心类**：`QueuingSimulator(lam, mu, c, service_time_cv)`  
实现“**M/M/c 基线 + M/G/c 修正**”的无限队列近似：

$$\rho = \frac{\lambda}{c \cdot \mu} \quad \text{（系统利用率，必须 < 1 才稳定）}$$

$$C(c, \rho) = \frac{\frac{(c\rho)^c}{c!} \cdot \frac{1}{1-\rho}}{\sum_{n=0}^{c-1}\frac{(c\rho)^n}{n!} + \frac{(c\rho)^c}{c!} \cdot \frac{1}{1-\rho}} \quad \text{（Erlang C，等待概率）}$$

$$W_q = \frac{C(c,\rho)}{c\mu(1-\rho)} \quad \text{（平均等待时间）}$$

$$W_q^{M/G/c} \approx W_q^{M/M/c} \times \frac{1 + CV^2}{2} \quad \text{（Lee-Longton / Allen-Cunneen 修正）}$$

项目中经验 $CV=0.365$，因此：

$$\text{correction factor} = \frac{1 + 0.365^2}{2} \approx 0.5666$$

接口会同时返回：
- `mean_wait_minutes`：M/G/c 修正后的等待时间
- `mean_wait_minutes_baseline`：M/M/c 基线等待时间
- `mgc_correction_factor`：修正系数

### `monte_carlo.py` — 财务风险仿真器

**核心类**：`MonteCarloSimulator(...)` → `run() -> MonteCarloResult`

循环 **1000 次**，每次独立采样：

```
每日服务次数  ~ Poisson(λ_eff × 24)
单次电量      ~ Normal(mean_kwh, std_kwh)，截断至 [1, 50] kWh
电价          ~ Normal(mean_e_price, σ×0.3)
服务费        ~ Normal(mean_s_price × (1+用户调参), σ×0.3)
购电成本      ~ Normal(wholesale × (1+用户调参), wholesale×0.1)
```

$$\text{Profit} = N \cdot Q \cdot (p_e + p_s) - N \cdot Q \cdot p_{wholesale} - C_{fixed}$$

本项目已将排队等待折损并入财务模型：

$$\text{Profit}_{new} = \text{Profit}_{old} - N \cdot W_q^{M/G/c} \cdot c_{wait}$$

其中 $c_{wait}=0.20$ 元/车·分钟，表示等待导致的隐性经营成本（流失、差评、补偿等）。

输出：`profits` 数组（1000 个）、`VaR_{5\%}`、均值、标准差、亏损概率、`mean_wait_penalty`

### `schemas.py` — 接口安检员

```python
class SimulationRequest(BaseModel):
    service_fee_change:       float  # [-0.9, 2.0]
    electricity_cost_change:  float  # [-0.5, 0.5]
    occupancy_change:         float  # [-0.9, 2.0]
       wait_cost_change:         float  # [-0.9, 2.0]
```

### `main.py` — FastAPI 主机

- `lifespan`：启动时调用 `load_baseline_data()` 预热缓存  
- CORS：`allow_origins=["*"]`（允许本地前端访问）  
- `POST /api/run-simulation`：接收 → M/G/c 排队修正 → MC 仿真 → 返回 JSON

---

## 前端展示层详解（frontend/）

### `index.html` — 页面骨架
- CSS Grid 双栏（控制面板 320px + 结果区自适应）
- 四个 `<input type="range">` 滑块（服务费 / 购电成本 / 客流量 / 等待惩罚成本）
- 五个 KPI 数字卡片（VaR / 期望利润 / 标准差 / 亏损概率 / 等待惩罚成本）
- ECharts 直方图容器 `<div id="profit-chart">`

### `style.css` — Apple 风格皮肤
- 全局 `#0d0f14` 深色 + 径向渐变光晕背景
- `.glass-card`：`backdrop-filter: blur(18px)` 毛玻璃效果
- 所有数字 / 图表：`transition: all 0.32s cubic-bezier(0.22,1,0.36,1)` 平滑过渡
- 正利润显示绿色，负利润/高风险显示红色（动态切换）

### `app.js` — 前端逻辑
1. 初始化 ECharts 直方图实例（深色透明背景，隐藏冗余网格线）
2. 四个滑块绑定 `addEventListener('input')`，防抖 380ms 触发 `fetch`
3. `fetch POST → /api/run-simulation` 携带 JSON 参数
4. 收到响应后：
       - 更新五个 KPI 数字（含颜色逻辑）
   - 调用 `buildHistogram()` 将 1000 个利润值分 32 个区间
   - 更新 ECharts 直方图 + VaR 参考竖线

---

## 补全参数汇总（★）

| 参数 | 值 | 依据来源 |
|------|----|---------|
| 等待折损成本 `wait_cost_per_minute` | **0.20 元/车·分钟** | 运营口碑折损与补偿的保守折现 |
| 批发购电价 `wholesale_price` | **0.55 元/kWh** | 深圳市发改委 2023 工商业一般用电峰谷均值 |
| 日固定成本 `daily_fixed_cost` | **300 元/站/天** | 星星充电/特来电财报运营成本行业中位数 |

> 修改方式：编辑 `backend/data_processor.py` 第 73-74 行对应值即可。

---

## 快速启动

```bash
bash start.sh
```

浏览器访问：`http://localhost:5500`

---

## API 接口文档

启动后访问：`http://localhost:8000/docs`（FastAPI 自动生成 Swagger UI）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 健康检查 |
| `/api/run-simulation` | POST | 核心仿真接口 |
